/**
 * @deprecated Ce fichier est un alias de compatibilité.
 * Utilisez directement "@/models" pour les nouvelles importations.
 *
 * Exemples :
 *   import type { CardHolder } from "@/models"
 *   import type { Card, CardStats } from "@/models"
 */
export type { CardHolder, CreateCardHolderDto, UpdateCardHolderDto } from "@/models/CardHolder"
export type { Card, CardStats, CreateCardDto, UpdateCardDto } from "@/models/Card"
export type { Organization, CreateOrganizationDto, UpdateOrganizationDto } from "@/models/Organization"
export type { Order, CreateOrderDto, UpdateOrderDto } from "@/models/Order"
